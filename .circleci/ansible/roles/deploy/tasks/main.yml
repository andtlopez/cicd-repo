---
- name: "extract backend files"
  become: yes
  unarchive:
    src: backend.tgz
    dest: /home/ubuntu

- name: Executing node
  shell: |
    cd /home/ubuntu/backend
    pm2 start npm --name backend -- start